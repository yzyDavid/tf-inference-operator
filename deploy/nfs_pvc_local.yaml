apiVersion: v1
kind: PersistentVolumeClaim

metadata:
  name: local-nfs-pvc

spec:
  accessModes:
    - ReadWriteMany
  volumeMode: Filesystem
  resources:
    requests:
      storage: 5Gi
  selector:
    matchLabels:
      content: models

